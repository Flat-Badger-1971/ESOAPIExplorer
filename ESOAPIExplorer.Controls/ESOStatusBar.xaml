<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ESOAPIExplorer.Controls.ESOStatusBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ESOAPIExplorer.Controls"
    xmlns:m="clr-namespace:ESOAPIExplorer.Models;assembly=ESOAPIExplorer.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    d:DataContext="{d:DesignInstance Type=m:StatusInformation}"
    mc:Ignorable="d">

    <Grid Grid.Row="1" Grid.ColumnSpan="2" Background="Black" Height="30">
        <StackPanel Orientation="Horizontal">
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="API Version: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding APIVersion}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Total Items: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding APIItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="C Functions: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding CFunctionItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Functions: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding FunctionItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Enum Types: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding EnumTypes}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Enum Constants: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding EnumConstants}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Events: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding EventItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Global Instances: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding GlobalInstanceItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="SI Globals: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding SIGlobalItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Objects: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding ObjectItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Object Methods: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding MethodItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="C Objects: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding CObjectItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="C Object Methods: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding CMethodItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Constants: " Foreground="{ui:ThemeResource SystemAccentColorLight1Brush}" />
                <Run Text="{Binding ConstantItems}" Foreground="{ui:ThemeResource SystemAccentColorDark1Brush}" />
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
