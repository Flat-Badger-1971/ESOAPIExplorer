<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ESOAPIExplorer.Controls.ESOStatusBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ESOAPIExplorer.Controls"
    xmlns:m="clr-namespace:ESOAPIExplorer.Models;assembly=ESOAPIExplorer.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=m:StatusInformation}"
    mc:Ignorable="d">

    <Grid Grid.Row="1" Grid.ColumnSpan="2" Background="Black" Height="30">
        <StackPanel Orientation="Horizontal">
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="API Version: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding APIVersion}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Total Items: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding APIItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="C Functions: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding CFunctionItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Functions: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding FunctionItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Enum Types: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding EnumTypes}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Enum Constants: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding EnumConstants}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Events: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding EventItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Global Instances: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding GlobalInstanceItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="SI Globals: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding SIGlobalItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Objects: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding ObjectItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Object Methods: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding MethodItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="C Objects: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding CObjectItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="C Object Methods: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding CMethodItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
            <TextBlock VerticalAlignment="CENTER" Margin="10,0,0,0" FontSize="10">
                <Run Text="Constants: " Foreground="{StaticResource SystemColorActiveCaptionColor}" />
                <Run Text="{Binding ConstantItems}" Foreground="{StaticResource SystemAccentColorDark1}" />
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
