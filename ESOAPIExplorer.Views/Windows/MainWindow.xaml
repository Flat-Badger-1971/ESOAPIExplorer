<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ESOAPIExplorer.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ESOAPIExplorer.Views"
    xmlns:vm= "clr-namespace:ESOAPIExplorer.ViewModels;assembly=ESOAPIExplorer.ViewModels" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Title="ESO API Explorer" Height="450" Width="800">

    <Grid x:Name="MainGrid" d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="Home" Command="{Binding NavigateCommand}" CommandParameter="HomeViewModel"/>
                <MenuItem Header="Export" Command="{Binding NavigateCommand}" CommandParameter="ExportViewModel"/>
                <MenuItem Header="About" Command="{Binding NavigateCommand}" CommandParameter="InfoViewModel"/>
                <MenuItem Header="Settings" Command="{Binding NavigateCommand}" CommandParameter="SettingsViewModel"/>
            </Menu>
            <Frame x:Name="NavFrame" NavigationUIVisibility="Hidden"/>
        </DockPanel>
    </Grid>
</Window>
